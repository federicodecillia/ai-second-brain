---
type: tasks
tags: [tasks, routines]
---

## For future agents
Recurring tasks (emoji 🔁). The Tasks plugin re-proposes them at the due date. The built-in one is [[maintenance]] — best scheduled (`docs/automation.md`) so it runs without you. Add your own recurring tasks below.

## Routines
- [ ] 🔁 every week run [[maintenance]] (or schedule it): file `00_Inbox/`, rescue `Daily/` upward, write the digest, health-check the vault
